// Keep this empty unless a specific official R5 Patient example is confirmed to
// be incompatible with the current structural validation scope.
export const r5PatientExampleSkips = new Map<string, string>([]);
