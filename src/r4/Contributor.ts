// Generated by scripts/generate.ts.
// Source: HL7 FHIR R4 OpenAPI schema from the pinned spec manifest.
import * as z from "zod";
import { ContactDetail } from "./ContactDetail";
import { Element } from "./Element";
import { Extension } from "./Extension";

export const Contributor = z.object({
	_id: z.lazy(() => Element).optional(),
	_name: z.lazy(() => Element).optional(),
	_type: z.lazy(() => Element).optional(),
	contact: z
		.lazy(() => ContactDetail)
		.array()
		.optional(),
	extension: z
		.lazy(() => Extension)
		.array()
		.optional(),
	id: z.string().optional(),
	name: z.string().optional(),
	type: z.enum(["author", "editor", "reviewer", "endorser"]).optional(),
});
